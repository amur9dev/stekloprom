# 📋 Инструкция для контент-менеджера

Данная инструкция описывает, как редактировать контент сайта «Стеклопром» без знания программирования.

---

## 🔧 Быстрый стар

### Что можно редактировать:
- ✅ Тексты заголовков и описаний
- ✅ Цены на продукцию
- ✅ Контактные данные (телефон, email, адрес)
- ✅ Список продуктов в каталоге
- ✅ Примеры работ (портфолио)
- ✅ Статьи блога
- ✅ Режим работы

### Что НЕ нужно трогать:
- ❌ Файлы в папке `node_modules/`
- ❌ Файлы конфигурации (vite.config.ts, tailwind.config.ts)
- ❌ Код компонентов (если не понимаете, что делаете)

---

## 📞 Изменение контактных данных

### Где менять:

**1. Главный файл с данными компании:**
`src/utils/seo.ts` — строки 38-58

```typescript
export const COMPANY_DATA = {
  name: 'Стеклопром',
  telephone: '+7 (863) 123-45-67',  // ← ТЕЛЕФОН
  email: 'info@stekloprom-rostov.ru', // ← EMAIL
  address: {
    streetAddress: 'ул. Промышленная, 15', // ← АДРЕС
    addressLocality: 'Ростов-на-Дону',
    // ...
  },
};
```

**2. Подвал сайта (Footer):**
`src/components/layout/Footer.tsx` — строки 19-25

```typescript
const CONTACT_INFO = {
  phone: '+7 (863) 123-45-67',      // ← ТЕЛЕФОН для отображения
  phoneHref: 'tel:+78631234567',    // ← ТЕЛЕФОН для звонка (без пробелов)
  email: 'info@stekloprom-rostov.ru',
  address: 'г. Ростов-на-Дону, ул. Промышленная, 15',
  workHours: 'Пн-Пт: 8:00-18:00, Сб: 9:00-15:00',
};
```

**3. Страница контактов:**
`src/pages/Contacts.tsx` — строки 15-25

---

## 💰 Изменение цен

### Популярные решения (главная страница):
`src/components/sections/PopularSolutions.tsx`

```typescript
const PRODUCTS = [
  {
    title: 'Однокамерные',
    price: 'от 1 200',  // ← ЦЕНА (только число и текст)
    // ...
  },
];
```

### Полный каталог:
`src/pages/Catalog.tsx`

Найдите массив `PRODUCTS` и измените поле `price`:

```typescript
{
  slug: 'odnokamernye',
  title: 'Однокамерные',
  price: 'от 1 200',  // ← ЦЕНА
  prices: [
    { title: 'Стандартный', formula: '4-16-4', price: '1 200' },
    { title: 'Улучшенный', formula: '4-18-4', price: '1 400' },
    { title: 'Премиум', formula: '6-16-4', price: '1 600' },
  ],
}
```

---

## 📝 Добавление статьи в блог

**Файл:** `src/pages/Blog.tsx`

Найдите массив `BLOG_ARTICLES` и добавьте новый объект:

```typescript
{
  id: 'novaya-statya-slug',  // URL-адрес (латиница, дефисы)
  title: 'Заголовок статьи',
  excerpt: 'Краткое описание для карточки (1-2 предложения)',
  content: `
    <h2>Подзаголовок</h2>
    <p>Текст параграфа...</p>
    <ul>
      <li>Пункт списка</li>
    </ul>
  `,
  image: 'https://images.unsplash.com/photo-...', // URL изображения
  date: '2024-02-01',  // Дата в формате ГГГГ-ММ-ДД
  readTime: '5 мин',   // Время чтения
  author: 'Имя Автора',
  category: 'Советы',  // Категория: Советы, Технологии, Сравнение
},
```

### HTML-теги для контента:

| Тег | Назначение | Пример |
|-----|------------|--------|
| `<h2>` | Заголовок раздела | `<h2>Заголовок</h2>` |
| `<h3>` | Подзаголовок | `<h3>Подзаголовок</h3>` |
| `<p>` | Параграф | `<p>Текст...</p>` |
| `<ul><li>` | Маркированный список | `<ul><li>Пункт</li></ul>` |
| `<strong>` | Жирный текст | `<strong>важно</strong>` |
| `<a>` | Ссылка | `<a href="/catalog">Каталог</a>` |

---

## 🏗️ Добавление проекта в портфолио

### Главная страница (6 работ):
`src/components/sections/CasesGallery.tsx`

### Полное портфолио:
`src/pages/Cases.tsx`

```typescript
{
  id: 13,  // Уникальный номер
  title: 'Название объекта',
  type: 'Жилой комплекс',  // Тип: Жилой комплекс, Бизнес-центр, Частный дом, и т.д.
  location: 'Район, Ростов-на-Дону',
  area: '1 500 м²',  // Площадь остекления
  year: '2024',
  description: 'Описание проекта в 1-2 предложениях.',
  image: 'https://images.unsplash.com/photo-...', // URL фото
},
```

---

## 🏷️ Добавление типа стеклопакета в каталог

**Файл:** `src/pages/Catalog.tsx`

Найдите массив `PRODUCTS` и добавьте новый объект по образцу существующих.

Обязательные поля:
- `slug` — URL (латиница, дефисы)
- `title` — Название
- `fullTitle` — Полное название
- `description` — Краткое описание
- `fullDescription` — Подробное описание
- `features` — Массив характеристик (3 штуки)
- `price` — Цена «от X»
- `icon` — Иконка (из Lucide React)
- `advantages` — Преимущества (4 штуки)
- `specs` — Технические характеристики
- `forWhom` — Для кого подходит
- `prices` — Варианты цен

После добавления в каталог, добавьте также в футер:
`src/components/layout/Footer.tsx` → `FOOTER_LINKS.products`

---

## 🖼️ Изменение изображений

### Где брать изображения:
- [Unsplash](https://unsplash.com) — бесплатные фото
- Собственные фото производства

### Формат URL Unsplash:
```
https://images.unsplash.com/photo-ID?w=600&h=400&fit=crop
```

- `w=600` — ширина в пикселях
- `h=400` — высота в пикселях
- `fit=crop` — обрезка по размеру

### Загрузка собственных изображений:

1. Положите файл в `src/assets/images/`
2. В коде используйте импорт:

```typescript
import myImage from '@/assets/images/my-image.jpg';
// ...
<img src={myImage} alt="Описание" />
```

---

## 🔍 SEO — изменение мета-тегов

### Главная страница:
`src/pages/Home.tsx` — строки 29-33

### Другие страницы:
В каждом файле страницы найдите вызов `setMeta`:

```typescript
setMeta({
  title: 'Заголовок (до 60 символов)',
  description: 'Описание для поисковиков (до 160 символов)',
  keywords: 'ключевые, слова, через, запятую',
});
```

### Правила SEO:
- **Title** — до 60 символов, главный ключ в начале
- **Description** — до 160 символов, призыв к действию, телефон
- **Keywords** — 5-10 ключевых слов через запятую

---

## ⚠️ Важные правила

1. **Сохраняйте кавычки** — все тексты должны быть в кавычках `'текст'` или `"текст"`

2. **Не удаляйте запятые** — после каждого элемента массива нужна запятая

3. **Проверяйте скобки** — открывающая `{` должна иметь закрывающую `}`

4. **Делайте бэкап** — перед изменениями сохраняйте копию файла

5. **Тестируйте** — после изменений проверьте сайт в браузере

---

## 🆘 Если что-то сломалось

1. **Откатитесь к предыдущей версии** через Git
2. **Проверьте консоль браузера** (F12 → Console)
3. **Сравните с оригиналом** — где пропущена запятая или кавычка?

---

## 📞 Техническая поддержка

При возникновении проблем обращайтесь к разработчику.
